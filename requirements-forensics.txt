# Clip-forensics OFFLINE research lane — concepts/clip-forensics-research-2026-06.
# NOT part of the base/live install. Install with:  pip install -r requirements-forensics.txt
# All picks are commercial-OK EXCEPT where flagged (offline analysis output is data only,
# but flags matter if any of these ever move into the live render path).
#
# Already present in the base env (do not reinstall): numpy, librosa, soundfile, torch, opencv (cv2).

# --- Phase 1: semantic audio sensing + scene cuts ---
panns-inference>=0.1.0        # MIT  — AudioSet CNN14 framewise SED. Downloads ~300MB ckpt to ~/panns_data on first use.
transformers>=4.40.0          # Apache-2.0 — CLAP zero-shot via ClapModel (laion/clap-htsat-unfused, ~600MB on first use).
# laion-clap>=1.1.6           # ALT CLAP backend (used automatically if transformers CLAP is unavailable).
scenedetect[opencv]>=0.6.4    # BSD-3 — scene-cut detection (ContentDetector).

# --- Phase 2: censor detection + music-bed segmentation ---
better-profanity>=0.7.0       # MIT  — profanity lexicon for predicted curse positions.
inaSpeechSegmenter>=0.7.6     # MIT  — music/speech/noise segmentation. NOTE: pulls in TensorFlow (heavy); install deliberately.

# --- Phase 3: caption OCR (visual) ---
easyocr>=1.7.0                # Apache-2.0 — burned-in caption OCR (preferred over PaddleOCR; see chrome-masking wedge history).

# --- Phase 4: exact known-SFX matching ---
# audfprint (Dan Ellis) — permissive; install from source/git when seeding an SFX reference library.
#   pip install git+https://github.com/dpwe/audfprint   (or use simple spectrogram cross-correlation)
# AVOID Panako (AGPL) for a monetized pipeline.

# --- Deliberately NOT used ---
# Essentia (+ MTG mood models): AGPLv3 + non-commercial models -> blocked for a monetized pipeline.
#   "suspenseful music" is a CLAP prompt instead (config/audio_sense_labels.json).
# Demucs: already present (vocal_sep.py); its pretrained WEIGHTS are research-only -> OFFLINE cross-check ONLY, never shipped.
