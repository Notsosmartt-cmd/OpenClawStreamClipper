{
  "text_model": "qwen/qwen3.6-35b-a3b",
  "vision_model": "qwen/qwen3.6-35b-a3b",
  "whisper_model": "large-v3-turbo",
  "llm_url": "http://host.docker.internal:1234",
  "context_length": 32768,
  "text_model_passb": null,
  "text_model_passd": null,
  "vision_model_stage6": null,
  "_phase_4_b5_note": "text_model_passd (Phase 4 B5 decorrelation): the model the Stage 4 text RUBRIC uses. Set it to a DIFFERENT family than text_model (e.g. 'google/gemma-4-12b-qat' when Pass B runs qwen) so the rubric's errors decorrelate from Pass B's — an independent second opinion, not a correlated echo. null (default) => falls back to text_model_passb => text_model (no decorrelation, no behavior change). On a 16 GB rig with OLLAMA_MAX_LOADED_MODELS=1 this forces a hot-swap around the rubric call, so only set it when LM Studio is up and the extra swap latency is acceptable.",
  "_phase_5_note": "Phase 5.1 split: `text_model_passb` and `vision_model_stage6` optionally override `text_model` / `vision_model` for those specific stages. When null (the default), they fall back to the unified model. Stage 3 segment classification always uses `text_model` regardless. Recommended split on a 48 GB rig: text_model_passb = 'qwen/qwen3-32b' (text-only, non-thinking, ~28 GB BF16, ~3x cheaper than multimodal for Pass B's classification workload), vision_model_stage6 = 'qwen/qwen3-vl-8b' (vision-specialist, ~10 GB FP8, stronger on Twitch OCR). The two can be co-resident; no hot-swap needed."
}