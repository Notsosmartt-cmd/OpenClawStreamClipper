{
  "version": 1,
  "_note": "Stage 5.5 Vision Judge (Plan 1.a). A seeded Swiss tournament of pairwise multimodal 'which clip is more engaging sound-off?' comparisons re-ranks the Pass C shortlist; win-count -> vision_rank + a BOUNDED raw_score reweight (+/- reweight_span) that propagates through Stage 6's raw_score sort. Never deletes a clip. Failure-soft: disabled / too-few moments / LM Studio outage / too-few comparisons all leave the Pass C order untouched. See AIclippingPipelineVault/wiki/concepts/clipping-quality-overhaul.md (Phase 1.a).",
  "enabled": true,
  "shortlist_min": 3,
  "shortlist_max": 12,
  "frames_per_clip": 4,
  "rounds_extra": 1,
  "max_comparisons": 20,
  "_max_comparisons_note": "C2 (plan-speed-wave3 2026-07-14): 30 -> 20. A Swiss tournament on a <=12 shortlist converges by round ~4 (~20 pairs); the last ~10 comparisons mostly reshuffled mid-table ranks that don't change the rendered set. JUDGE_MAX_COMPARISONS env or this key reverts.",
  "reweight_span": 0.25,
  "per_pair_timeout_seconds": 60,
  "max_tokens": 1200,
  "stage_timeout_seconds": 600,
  "fail_streak_limit": 3,
  "workers": 2,
  "_workers_note": "Fix 2B (2026-06-06): parallel pairwise-judge calls within each Swiss round (rounds still re-rank sequentially). 2 matches Stage 6's cap for the shared LM Studio vision model on the split NVIDIA+AMD pool; the encoder may serialize so >2-3 yields little. JUDGE_WORKERS env overrides. To skip Stage 5.5 entirely (it only re-orders a set that renders in full), set enabled=false."
}
