---
title: "HANDOFF — Pipeline Upgrade Execution State (2026-07-03)"
type: concept
tags: [handoff, execution, roadmap, phase-runner, anomaly-lane, status, agent]
sources: 0
status: in-progress
updated: 2026-07-04
---

# HANDOFF — Pipeline Upgrade Execution (2026-07-03)

**For the next agent.** Self-contained state + how to continue executing [[concepts/plan-pipeline-upgrade-2026-07]]. Read this, then that plan (has per-phase files/flags/acceptance-criteria), then [[concepts/master-research-2026-07]] (parameter sources). Resume point on disk: `phase_state.json` in the work dir (currently `val_rakai=PASS`, phase 0 complete).

## Binding rules (repo law — do not violate)
Flag-gated (default OFF) · failure-soft (missing dep → prior behavior) · **boost-only** (new lanes ADD candidates/signal, never gate/evict) · storytime + blatant-banger lanes untouched · **flag-off run must be byte-identical to baseline on `hype_moments.json`** (deterministic artifact — NOT rendered bytes; NVENC is non-deterministic) · every gated run executes through `phase_runner` and records a `run_eval_<id>.json` verdict · wiki page + log/hot + commit per phase · bound every heavy run (no zombie tasks).

## Environment facts (verified 2026-07-03 — save yourself the recon)
- **Two Python environments, do not confuse them:** the pipeline runs in **`.venv\Scripts\python.exe`** (torch 2.8.0+cu128, CUDA); the forensics/harness deps (CLAP, EasyOCR, scenedetect — torch 2.9.1+cu130) are in **system Python 3.12**. `phase_runner` launches the pipeline with the venv python automatically; run the *harness itself* with system python.
- **sentence-transformers is NOT installed** anywhere → `meme_match.py` is lexical-only by design.
- 5 real VODs in `vods/` (transcriptions cached → runs skip Stage 2 unless `--force`). `20260424_2xRaKai` = the rap-battle/Delaware VOD.
- LM Studio stays UP (owner commitment): `qwen/qwen3.6-35b-a3b` (pipeline) + `google/gemma-4-12b-qat` (Phase 4 decorrelation) both loaded.
- Work dir: `C:\Users\user\AppData\Local\OpenClawClipper\work` (markers, `hype_moments.json`, `phase_state.json`, `run_eval_*.json`). Diagnostics: `clips/.diagnostics/last_run_<stamp>.json` (NOT `*_diagnostics.json`; `axis_report`/`hype_moments`/`clips_made` are keys inside it; `clips_made` is a LIST of titles).

## The harness — how to run any gated phase (unattended)
```
# system python:
python scripts/research/phase_runner.py launch  --vod NAME --force --profile validation --label RUN --phase N
# then a bounded background watcher (Bash run_in_background, until-loop on `status`) — wakes you on done/fatal/timeout
python scripts/research/phase_runner.py status   --run RUN
python scripts/research/phase_runner.py evaluate --run RUN --phase N --forensics [--baseline RUN0]
python scripts/research/phase_runner.py state    --advance PASS|FAIL --phase N
```
`launch` is fully detached (survives the agent sandbox). A run ≈ 30–45 min; launch it, start the watcher, build the NEXT phase's code while it runs, evaluate when woken.

---

## DONE (committed — verify with `git log --oneline`)
- ✅ **Phase 0.0** `scripts/research/phase_runner.py` — the harness. Proven on a real run.
- ✅ **Phase 0.1** — 44-min rakai validation run → `evaluate` **PASS 6/6** (10 clips, loudness −20.73 LUFS, forensics-on-output dogfood). Cleared the SFX-render + detection-fix 🟡s. Baseline saved: `work/hype_moments_val_rakai.json`. **Owner reviewed the clips (2026-07-04): quality good, flash timing "alright" — the perceptual gate is also green.**
- ✅ **Phase 1 MODULES** `scripts/lib/event_timeline.py` + `scripts/lib/anomaly_propose.py` — unit-verified (George-Bush proposes; precision controls + few-shot verifier gate). **Live Stage-4 wiring NOT done.**
- ✅ **Phase 5** `config/meme_formats.json` (12 formats) + `scripts/lib/meme_match.py` — precision-first, unit-verified. (Not yet consumed by any stage — Phase 3 wires it.)
- ✅ **Phase 2 CORE** `scripts/lib/chat_mine.py` — overlay-OCR path (velocity/dedup/7s-lag), pure logic verified. **Real-frame ROI needs an UPGRADE before verification — see LEFT.**
- ✅ **Effects manifest (2026-07-04, owner request)** — `scripts/lib/effects_log.py` → `clips/.diagnostics/effects_log.jsonl`; hooks in `profile_render.py` (SFX kind+t+gain), `clip_cuts.py` (flashes), `stage7._maybe_cold_open` (teaser). **Logging-only — never let a change break or bypass these hooks.** Reader: `python scripts/lib/effects_log.py [--clip X]`. The 0.1 run predates it; **the next run populates it — report the placement list to the owner** (he's waiting to critique SFX timing).

## LEFT (in execution order)

### NEXT: Phase 1 live wiring (the keystone payoff)
Wire the tested modules into detection. Steps:
1. In Stage 4 (`scripts/lib/stages/stage4_moments.py`), after Pass B+/before Pass C, behind **`CLIP_ANOMALY_LANE`** (default OFF): build the timeline (`event_timeline.build_timeline`) from the run's transcript + `audio_sense.sense_events` over the VOD audio (EXPENSIVE — 2 h VOD; gate it, cache to `{work}/timeline.json`, respect the watchdog pattern) + `visual_sense.motion_events` + cuts; call `anomaly_propose.propose(...)` with `keyword_fn` = the window's Pass-A keyword score and `verify_fn=anomaly_propose.verify_via_lmstudio`; inject survivors into Pass C candidates as `src=ANOMALY` (boost-only).
2. **Gotcha:** running CLAP over a 2 h VOD is the cost — consider only scoring windows near Pass-A/audio-event peaks, not the whole VOD. Decide + document.
3. **Verify via harness:** (a) flag-OFF run → `hype_moments.json` must equal `work/hype_moments_val_rakai.json` (the baseline) — use `evaluate --baseline val_rakai`; (b) flag-ON run on rakai → does a bus/George-Bush/rap-beat moment appear that wasn't there before? (c) laughter-anchored recall auto-eval.

### Phase 3 — judge timeline + known_format probe (small; replay-verifiable)
- Feed `event_timeline.render_for_prompt` window into `scripts/lib/vlm_judge.py` (`_clip_text_block`) behind `CLIP_JUDGE_TIMELINE`.
- Add `known_format:{name,confidence}` + wordplay check to the judge/Stage-6 prompt; pre-fill via `meme_match.match(transcript, visual_tags, audio_labels)` and let the LLM confirm/name. Thread into title/hook.
- Verify by replaying on the rakai run's cached `judge_tournament.json` / frames (no full run needed).

### Phase 4 — calibration loop + decorrelation (~2 d, CPU)
- **B1** cache Pass B raw output pre-Pass-C (small stage4 change) → needs ONE instrumented run to produce it. **B2** offline re-scorer CLI. **B3** grid-search fitter → `config/selection_axes_fitted.json`. **B4** logistic ranker + interaction features (`motion_high×words_banal`, anomaly features). **B5** (independent, ~2 h) `text_model_passd=google/gemma-4-12b-qat`, thread through `stage4_rubric.py`/`stage5_5_judge.py`.
- Labels: `bootstrap_twitch_clips` triples + RQ3's community-reel harvesting.

### Phase 6 — omni verifier (CONDITIONAL on the 0.2 smoke test)
- **0.2 smoke test first:** get a Vulkan llama.cpp build, `llama-server -hf ggml-org/Qwen3-Omni-30B-A3B-Instruct-GGUF`, POST audio to `/v1/chat/completions`. Watch for the AMD-mmproj FPE (`--no-mmproj-offload`) + experimental-audio 400s (RQ1 risk flags). GO → build a swap-in judge phase (never co-resident with the 35B). NO-GO → skip, zero plan impact.

### Also outstanding
- **0.3 verification debt:** resume `wf_edb4d979-c18` (deep-research verify layer) after the Anthropic session-limit reset to vote on the 114 unverified RQ claims ([[concepts/master-research-2026-07]]).
- **Phase 2 real-frame work = UPGRADE `detect_chat_roi` FIRST, then verify** (owner constraints 2026-07-04, bound in the module docstring): the shipped single-global-box detector does NOT yet meet them. Required: **windowed re-detection** (ROI moves mid-stream — plaqueboymax top-left→middle-left), **counter rejection** (follower/gift counters like "8589/9000" are small changing text too — require a multi-line ≥3-row stack, mostly non-numeric tokens, high line-turnover), no scroll-speed prior, variable text size. **ALL owner VODs have burned-in chat** → verify on any of the 5 (plaqueboymax = the moving-ROI stress case); the no-chat negative test needs a chat-less YouTube MP4 (owner will drop some) or a reference clip. EasyOCR is in system python.
- **Delaware-battle check by TIME:** the good rap-battle clip at T=9685 is NOT the Delaware battle; "delaware" appears in ZERO candidate pools of the 0.1 run. Pull the battle's timestamp from [[concepts/case-rap-battle-missed]] and inspect the diagnostics pools by time window — was it detected-and-dropped (Phase 4 ranker evidence) or never detected (Phase 1 lane evidence)?
- **sfx-anchor firing check (small):** the 0.1 log has NO `sfx-anchor:` lines — the acoustic-anchor path (`sfx_cues.build`) may have returned empty and fallen back to profile-synthesized cues. The effects manifest will show the cues used next run; if anchoring isn't firing, diagnose `sfx_cues.build` (transcript laughter markers present?).

## Gotchas this session already hit (don't re-discover)
- Detached launch needs Windows `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP|CREATE_BREAKAWAY_FROM_JOB` or the sandbox reaps it.
- Diagnostics filename + embedded keys (see Environment facts) — the grader was fixed for these.
- `--force` re-runs Stage 2 even with cached transcription (~15 min). Omit `--force` to reuse the cache if you don't need a fresh transcript.
- The recurring **deep-research verify-layer crash on session limits** — synthesize manually from fetched claims if it happens again (established playbook).

## Related
- [[concepts/plan-pipeline-upgrade-2026-07]] · [[concepts/master-research-2026-07]] · [[concepts/master-proposal-2026-07]] · [[concepts/case-incongruity-comedy]] · [[concepts/reference-humor-2026-07]] (bound-every-run discipline lives in the `bound-background-tasks` auto-memory, not the wiki)
