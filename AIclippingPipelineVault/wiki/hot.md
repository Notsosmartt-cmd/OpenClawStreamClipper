---
title: "Hot — current state & recent activity"
type: overview
tags: [hot, hub, status]
updated: 2026-07-11
---

# Hot

Bounded current-state digest (cap ~100 lines — when you add, prune).
Rewritten freely; [[log]] is the append-only record, this is the cache.
Update this whenever you prepend a [[log]] entry: add the one-liner, drop
anything stale (>~2 weeks), and refresh the state table if defaults/flags/models changed.

## State snapshot
| Fact | Value | Since | Detail |
|---|---|---|---|
| Default architecture | bare-metal Windows (Python orchestrator) | 2026-06-04 | [[concepts/bare-metal-windows]] |
| Orchestrator entry | `scripts/run_pipeline.py` (`--vods` / `--all` / `--force`) | 2026-06-04 | [[concepts/clipping-pipeline]] |
| text_model | qwen/qwen3.6-35b-a3b (unified MoE, ~3B active) | 2026-06 | [[entities/qwen35]] |
| vision_model | qwen/qwen3.6-35b-a3b (same unified model) | 2026-06 | [[entities/qwen3-vl]] |
| Context length | 32768 | 2026-06 | [[concepts/vram-budget]] |
| Transcription | faster-whisper large-v3-turbo, word-level SRT | 2026-06 | [[entities/faster-whisper]] |
| Captions | CapCut word-box, bundled Montserrat Black | 2026-06-06 | [[concepts/captions]] |
| Stage 7 encode | h264_nvenc by default (libx264 fallback) | 2026-06-06 | [[concepts/clip-rendering]] |
| Audio-events scan | threaded default (min(4,cores-2), byte-identical 3.3×) + per-VOD cache (re-runs skip) | 2026-07-08 | [[concepts/pipeline-speed-findings-2026-07]] |
| Speed scorecard (Wave 1) | GREEN #1/#2/#7 live; RED #3/#5/#6 archived (default-off = not integrated) | 2026-07-09 | [[concepts/plan-pipeline-speed-2026-07]] |
| Speed scorecard (Wave 2) | **C3 + C4 + C1 DEFAULT-ON** (C4 gate hit 3.7s/miss 100s; C1 contention +0.1%, saves ~5.6min/VOD-transition); C2b DELETED (marginal); **S1 RED (8× regression)**; C6 closed | 2026-07-09 | [[concepts/plan-serving-stack-2026-07]] §0 |
| Serving bench facts | decode 50 tok/s; prefill ~42%/call; KV prefix-reuse ON + survives alternation (PARALLEL=4) | 2026-07-09 | [[concepts/pipeline-speed-findings-2026-07]] §9b |
| Run timing | median 0.26× realtime; moment-parallel **DEFAULT-ON** (1.15× Stage-4, `CLIP_PASSB_MOMENT_WORKERS=2`, kill =1); spec-decode 0.12×/cards 4% dead; beyond it only hardware/model-tier | 2026-07-09 | [[concepts/pipeline-speed-findings-2026-07]] §7b/§9b |
| Caption quality gate | `CLIP_CAPTION_JUDGE`/`CLIP_CAPTION_LINT`/`CLIP_CAPTION_VOICE` **ON** (fidelity judge + AI-tell linter on title/hook) | 2026-07-10 | [[concepts/plan-captions-and-ab-variants-2026-07]] |
| A/B + post-kit + profiles | `CLIP_AB_VARIANTS=2` + `CLIP_POST_KIT` + `CLIP_STYLE_PROFILES` **ALL DEFAULT-ON** (owner 9/9-GOOD promotion); post kits → `clips/post_kits/` | 2026-07-10 | [[concepts/plan-captions-and-ab-variants-2026-07]] |
| Latest bug | BUG 66 (Stage-6 rebuild dropped primary_pattern → P-TIGHT rap exemption never fired, fixed) | 2026-07-08 | [[concepts/bugs-and-fixes#BUG 66]] |
| Docker | legacy, superseded by bare-metal | 2026-06-04 | [[concepts/bare-metal-windows]] |

## In flight / awaiting validation
- **Clip-forensics SHIPPED — Phase 1-3 + 4b + watchdog (2026-06-21)** — offline decomposer is functionally complete: `audio_sense.py` (CLAP default; PANNs opt-in) + `visual_sense.py` (cv2 motion + EasyOCR captions) + `clip_forensics.py` (cuts/censor/music-bed/motion/captions + **LLM style_profile**). **Verified on `ReemKnocksClip.MP4`:** CLAP 14 events, whisper 18 words, 6 cuts, 9 motion spikes, OCR 13.08 wps (real text), LLM essence coherent (122 s). Install records + env caveats: **[[entities/audio-sense-module]]**, **[[entities/visual-sense-module]]**. **Hang-proof watchdog:** every stage under `_with_deadline` (cap → abandon → partial result; `_stages` status); total runtime ≤ sum of caps. Defaults: PANNs opt-in (torch≥2.9 stall), CPU default, EasyOCR `--ocr` opt-in, LLM `--no-llm` opt-out. Phase 4a exact-SFX deferred (needs seeded SFX lib). Plan [[concepts/plan-clip-forensics]] (shipped). *Open: CLAP cosines run low (~0.26–0.32) → `clap_threshold` 0.30, calibrate per-corpus vs `reference_clips/*.notes.json`.*
- **SFX-anchor VALIDATED on rakai (2026-07-04, p4cal run)** — post `_bool_env` fix the acoustic anchor fires (10 `sfx-anchor:` lines, boom cues placed). Cold-open teaser now COMPUTES windows too but its attach hit **BUG 65** (cross-drive move, fixed) — a re-run is needed to see teasers actually land in the clips. Owner-critique fixes (adaptive gain + onset-snap + cold-open) VALIDATED on the L0 run 2026-07-05 (per-clip boosts 0→+4.9 dB, 5 cold-opens attached, beat-anchored SFX) — pending the owner's ears. Manifest "1/10 coverage" was a run-stamp grouping artifact (fixed via `CLIP_RUN_STAMP`) — coverage was actually full — [[concepts/sfx-cue-taxonomy-2026-06]] · [[concepts/bugs-and-fixes#BUG 65]]
- **Detection fixes exercised on rakai (2026-07-03)** via harness run w/ `CLIP_SEGMENT_VOTES=3` + arc-ratio 0.45 → 10 clips incl. a rap-battle clip; deeper Delaware-bucket confirmation is a follow-up read of the diagnostics — [[concepts/clipping-intelligence]] §Opportunity D · [[concepts/plan-pipeline-upgrade-2026-07]]
- Transition animations (white-flash + jump-cut compression) shipped flag-gated; BUG 64 fix lands the flash — needs a clean validation run (`CLIP_JUMP_CUTS=gaps` safest first) — [[concepts/transition-animations]]
- Arc-stitch FORMED groups in production but stays inert at default ratio on rich VODs — lower `CLIP_ARC_GUARANTEE_MIN_RATIO` (~0.45) to give arcs a slot — [[concepts/arc-aware-extraction]]
- 4 detection fixes shipped flag-gated/failure-soft; Fix 4 (length-neutral) strongly validated, Fixes 2/3 not yet exercised on a known-arc VOD — [[concepts/detection-improvements-plan]]
- Fix 2 finding: short category prototypes only mildly discriminative (cosine ~0.15–0.27); follow-up is richer `config/patterns.json` signatures — [[concepts/detection-improvements-plan]]

## Recent changes (last ~10, one line each, newest first)
- [2026-07-11] **VERIFIED: SFX density 1.1→4.9 cues/clip (measured) + sentence-case confirmed on frame**; news A/B compiled after the concat fix (fingerprint gives clips jittered sizes 1076×1916/SAR 10240:10239 → every compilation piece now normalizes to exact 1080×1920 SAR 1:1). Diversity swap observed live ([comedy] between two [hype]). Formal re-diff in background — [[log]]
- [2026-07-11] **NEWS v1.5** (owner directives): news-weighted selection (controversial ×1.35/storytime ×1.2 + diversity swaps), **`chk-news-after` toggle**, **A/B compilations**, **reorg-proof recursive lookup** (owner moved clips into subfolders/off-repo) — [[concepts/plan-news-compilation-2026-07]] — [[log]]
- [2026-07-11] **R4 APPLIED + NEWS v1 SHIPPED**: SFX density (max_cues 6, funny/reactive risers, `secondary_peaks` roast-cadence scanner — self-test 5 cues vs old 1) + TRUE sentence-case captions (`CLIP_CAPTION_CASING`, whisper Mid-Caps fixed) — both live NEXT run. **`news_compile.py` + dashboard `News Compile (N)` button**: finished-clips sub-cuts + grid intro + banners + **piper VO** (en_US-ryan-high, installed) → first compilation `STREAMERS UPDATE 07-11-2026.mp4` (58.9s, 4 stories) — **owner ear-check gate open** — [[concepts/plan-news-compilation-2026-07]] — [[log]]
- [2026-07-11] **First curation pass on report #1**: chat-overlay gap line REJECTED (artifact — VLM counted the STREAM's own chat; card schema v2 splits source/added; `diff_approvals.json` queue live). News decisions: separate button ONLY + piper VO to v1 — [[log]]
- [2026-07-11] **LOOP CLOSED — first diff report on the owner's desk** (`clips/.diagnostics/corpus_diff_20260711.md`, 13 lever-tagged items): headline = **our clips are SFX-sparse vs the corpus** (0.88 cues/30s ground truth) — the loop independently re-derived the owner's own "I wish there was sound effects" critique from data; + caption casing `mixed` vs ref `sentence case` (P1.5 lever) + coverage misses (news_compilation → the new mode). Measurement asymmetries filed (ref SFX = raw CLAP inflated; our caption-wps = speech fallback — fix from our SRT). R2 = 9/9 cards w/ ground truth (prefix-match fix: filenames are `[:50]`-truncated titles) — [[concepts/plan-reference-deconstruction-2026-07]] — [[log]]
- [2026-07-11] **R1 COMPLETE (59 cards, 7 real categories) + R2/R3 tools SHIPPED**: `our_clip_cards.py` (run-scoped cards w/ `effects_log` `_ground_truth`; stamp gotcha: effects=START `20260710_143929` vs diagnostics=END `_202308`) + `corpus_diff.py` (per-category ref-vs-ours, 25% threshold, `lever:`-tagged, `.md`+`.json`) — [[log]]
- [2026-07-11] **R1 gate CLEARED + "News Today" mode filed**: owner passed the 6 cards (StreamerUpdate "great outputs" → inspired [[concepts/plan-news-compilation-2026-07]] — multi-VOD → one grid-intro/list-arc compilation, format spec measured FROM the cards, ~75% reuse, v1 1-2 sessions); George-Bush card good sans external joke context (by design); taxonomy refined to 10 buckets — [[log]]
- [2026-07-11] **R0+R1 reference-deconstruction started**: `scripts/research/attribute_cards.py` SHIPPED — one multimodal call/clip, **VLM reads on-screen text from frames** (bypasses EasyOCR) + Python-authoritative numerics → `.cache/<stem>.card.json` (gitignored). 6 spot-read cards built; George-Bush hook read CLEAN ("you ever heard of george" — the exact string EasyOCR poisoned voice-v1 with). **R0 DONE: 59/59 timelines** (survived a mid-run PC crash, resumed clean, all valid). **Gate (open):** owner spot-reads 6 cards + confirms category granularity (all 6 = "irl", maybe too coarse for R3 diff) BEFORE the full 59-card batch / R2 — [[concepts/plan-reference-deconstruction-2026-07]] — [[log]]
- [2026-07-10] **Learning evaluated → [[concepts/plan-reference-deconstruction-2026-07]] filed (+R6 Reference Lab tab)**: owner's clips-in→attributes-out inversion endorsed (~70% exists as clip_forensics); new layers = editorial CARDS (VLM reads on-screen text, not EasyOCR) → contrastive DIFF vs our clips (`lever:`-mapped) → curated apply → `CLIP_REF_FEWSHOT` retrieval → **"Reference Lab" dashboard tab** (corpus table, execute buttons, card/report viewers, approve-per-line; mutual 409 w/ pipeline via pid marker). **Loop A de-emphasized**; **owner doctrine: NO training/fine-tuning ever** — [[log]]
- [2026-07-10] **Owner review 9/9 GOOD → A/B + post-kit + style-profiles PROMOTED DEFAULT-ON**: env defaults flipped (`run_pipeline`, stage6/stage7, `pipeline_env`, routes) + dashboard checkboxes pre-checked + `DEFAULT_ORIGINALITY.style_profiles=true`; kill switches kept. Post-kit sidecars moved to **`clips/post_kits/`** (clips root stays video-only; 9 existing files moved). **9 positive labels filed → owner pool 35 (33/2)**, run `20260710_202308` frozen — [[concepts/plan-captions-and-ab-variants-2026-07]] — [[log]]
- [2026-07-10] **A/B + post-kit VALIDATED on a full run** (2xRaKai `20260710_202308`, exit 0, 43m49s): 9 primary + **5 (B) variants** (A/B mp4 sizes differ → `--seed-offset` really varied B's SFX/visual) + **9 `.post.json`** (no hashtags, A/B hooks, `trial_reel`). **Caption gate caught a live mismatch** (T=6799 title nulled fidelity 2/10 — chased a minor topic over the clip's climax) and ONLY that one nulled all run; titles else = natural lowercase voice. +~6 min over the 38-min baseline. Owner step: eyeball B-vs-A → default-on decision — [[concepts/plan-captions-and-ab-variants-2026-07]] — [[log]]
- [2026-07-10] **Caption overhaul + A/B variants IMPLEMENTED (full plan, mechanic-validated)**: **Part 1 caption fixes DEFAULT-ON** (kill switches) — full-window transcript (`[:500]`→`[:4000]` for gen AND grounding), `grounding.caption_judge` fidelity gate + `caption_lint.py` AI-tell linter wired into Stage-6 `_ground_field`→`_caption_gate` (the semantic check `title`/`hook` NEVER had — was Tier-1 only), voice-contract prompt (`CLIP_CAPTION_VOICE`). caption_judge live-scored on-topic **fidelity 10** vs off-topic **0** ("rap battle that never happens"). **Part 2 A/B DEFAULT-OFF** (`CLIP_AB_VARIANTS`, dashboard `chk-ab-variants`) — variant B = full `profile_render` w/ `--seed-offset` (varied SFX+visual, beats still anchored) + `.post.json` platform kit (no hashtags). Owner gates: Part-1 eyeball, P1.5 voice-bank curation (`--review-sheet`→`--ingest-sheet --enable`), Part-2 variant run — [[concepts/plan-captions-and-ab-variants-2026-07]] — [[log]]
- [2026-07-10] **Owner review → BUG 68 FIXED + boom pool tightened + 6 labels**: A1 arc lane clipped the end-of-stream DEAD-AIR screen (0-word window, vision judge passed it 0.778!) → dead-air guard at Pass-C (`CLIP_MIN_CLIP_WORDS=8` default-on, validated vs real trace: drops exactly the bad clip). Boom = ddg+vine only (CC0 impacts "too quiet"). 5 pos + 1 NEG labels (pool 26; 2nd real negative). Coke Machines + Gym Class = clean passes. New cases: moment-splitting (2 payoffs in one window), visual-subtext blind spot — [[concepts/bugs-and-fixes#BUG 68]] — [[log]]
- [2026-07-09] **SFX seeded + guide + review run DONE (10 clips, 37.6 min — fastest yet)**: boom kind (DDG boom trimmed 2.5s, vine boom); NEW pop kind (laughter: boing/oof/quack/dry_fart) + fart kind. Loader gotcha: manifest folders ONLY use manifest entries. Guide: [[concepts/sfx-cue-taxonomy-2026-06]] §HOW-TO — [[log]]
- [2026-07-09] **Session handoff filed** → [[concepts/handoff-2026-07-09]] (capstone: speed scorecard, BUG 67, moment-parallel, thinking control, companion shorts, doctrine). **Companion-shorts dashboard toggle** wired (`chk-companion-shorts` → `CLIP_COMPANION_SHORTS`, env-chain verified) — [[log]]
- [2026-07-09] **Companion punchline-only shorts** (`CLIP_COMPANION_SHORTS`, default off, **dashboard checkbox**): for long clips (≥30s) with a late payoff, `stage7.py` also emits `"<title> (Short).mp4"` — a payoff-centered sub-cut of the FINISHED clip (captions/effects inherited + aligned, no re-render). Owner's 'Yo! Freestyle' idea. Exempt storytime/emotional; verified 36s→11s live — [[concepts/clip-rendering]] — [[log]]
- [2026-07-09] **Moment-parallel PROMOTED DEFAULT-ON (spot-check: 4 good/2 needs-work/0 bad)**: `CLIP_PASSB_MOMENT_WORKERS` default **2** (kill switch =1) — 1.15× Stage-4, ~30 min/Lacy-class VOD. 4 positive labels filed+frozen (owner pool now 20). Review themes for the QUALITY lanes: caption/title↔video mismatch (Stage 6), trim-vs-punchline precision, owner wants SFX/effects at payoff beats (sfx-anchor lane candidate), NEW idea: companion punchline-only short — [[concepts/plan-serving-stack-2026-07]] §0 — [[log]]
- [2026-07-09] **Double-launch GUARDED + gemma re-tested + Stage-4 timeline corrected (BUG 67)**: `is_pipeline_running()` now cross-process (on-disk pid marker) so a restarted/2nd dashboard's play returns 409; `api_stop` kills via marker. Gemma re-test (owner toggled thinking off): 0 reasoning in ALL rows incl. `enable_thinking=True` → gemma obeys the MODEL-level toggle, ignores the per-request kwarg (qwen proves LM Studio forwards it → it's gemma's template). Timing: Lacy = 5h52m (it's a 6.8h VOD, ~normal); Jynxzi crippled-not-cleanly-failed, never finished — [[concepts/bugs-and-fixes#BUG 67]] — [[log]]
- [2026-07-09] **Thinking toggle + fail-fast guard SHIPPED (BUG 67 fix)**: verified pipeline uses the right no-think commands (`/no_think` + `chat_template_kwargs` at all 6 LLM sites); proved qwen obeys (0 reasoning), gemma ignores the REQUEST kwarg but IS controllable at the template level (fresh load=0, works — earlier "permanent" claim was measured with thinking toggled ON). Shipped `scripts/lib/thinking.py` + `CLIP_ENABLE_THINKING` dashboard checkbox (default off, byte-identical) + `common.preflight_thinking()` guard (probe reasoning_tokens>50 → abort in ~1s instead of 8h). Validated qwen=0 pass / mock 200 abort — [[concepts/bugs-and-fixes#BUG 67]] — [[log]]
- [2026-07-09] **INCIDENT (BUG 67): 8h wedged 3-VOD batch + PC crash — 2 root causes**: (A) `google/gemma-4-26b-a4b-qat` (owner's uncommitted Lever-2 model swap) is a THINKING model that ignores `/no_think` → burns all 8000 tokens on reasoning (`finish=length, reasoning_tokens=7997`, 379× empty-content) → 100% Stage-4 failure; (B) TWO dashboard instances (`.venv` + `Program Files` python) double-launched the same batch → shared work dir (marker clobber = phantom "stuck") + shared LM Studio (contention). Reverted models.json→qwen, cleared stale markers, cleaned temp. No git data loss. `run_pipeline.py` concurrency guard = TODO — [[concepts/bugs-and-fixes#BUG 67]] — [[log]]
- [2026-07-09] **Dashboard inherits Wave-2 defaults (verified) + 8× mechanism documented**: dashboard bare-metal path runs `run_pipeline.py` with `os.environ.copy()` + no speed-flag overrides → C3/C4/C1 all auto-on (C1 only on batch/`--all`/multi-select; single-VOD = no-op). Docker path bypasses ALL Python-pipeline wins. 8× spec-decode = economics inversion (`draft_cost≈target_cost` on the coordination-bound split; catch-22 needs single-card fit) — [[concepts/plan-serving-stack-2026-07]] §0a, [[concepts/pipeline-speed-findings-2026-07]] §9b — [[log]]
- [2026-07-09] **Speed Wave 2 IMPLEMENTED + benched**: built flag-gated C3 (reload hygiene, `stage2.py`, default-on behavior-preserving), C4 (`CLIP_SEGMENT_CACHE`, stage3 cache, self-test 15/15), C2b (`CLIP_PROMPT_STATIC_FIRST`, static-first prompt), C1 (`CLIP_BATCH_PREFETCH`, isolated next-VOD Stage-2 prefetch, self-test 11/11) + P0 tools (`bench_serving.py`, `retry_audit.py`, `CLIP_PASSB_DUMP_PROMPTS`). Benched: decode **50 tok/s**, prefill ~42%/call, **KV prefix-reuse already ON + survives alternation** (PARALLEL=4) → C2b justified. **C6 CLOSED** (0.49% retry, 0 parse-fails). **S1 spec-decode ⛔ GUI-ONLY** — CLI flag rejected on Vulkan + API `draft_model`=400; draft qwen3.5-2b staged, owner GUI recipe filed, gain UNMEASURED — [[concepts/plan-serving-stack-2026-07]] §0/§3a — [[log]]
- [2026-07-09] **Speed Wave 2 UNIFIED plan (execution-grade)**: serving-stack + C1–C6 combined into one 6-phase plan w/ file:line anchors + gates. Key discoveries reshaping it: `stage2.py:34` unloads ALL models every VOD (even cached re-runs) → 35B cycles per-VOD → **S1 draft flags must integrate in `common.load_model()` (common.py:267)**, not a pre-run load; Stage 3 is LLM-voted → C1 redesigned as early-evict-after-S6 + prefetch next VOD's full Stage 2 into the ~338s render window (~4–6 min×(N−1), fresh batches). MTP dead on current GGUF; S1 = qwen3.5-2b draft (vocab 248320, weights untouched); flash-attn already ON; GPU split not tunable (no-go); disk NVMe/ReBAR ON → closed — [[concepts/plan-serving-stack-2026-07]] — [[log]]
- [2026-07-09] **Wiki reorganized to the strict speed scorecard (owner rubric: default-off = RED)**: speed plan → shipped w/ FINAL SCORECARD (GREEN #1/#2/#7 live + #4 pre-existing; RED #3/#5/#6 archived); #5/#6 exec plan → retired w/ archive pointers; hot state table gained audio-scan-defaults + scorecard + LLM-floor rows — [[concepts/plan-pipeline-speed-2026-07]] — [[log]]
- [2026-07-09] **bug66-confirm KILLED (owner call) → fix verified by executable code inspection**: 4-hop chain proven (S4 emit → S6 preserve → S7 row → `_exempt`; dict-flow sim w/ JSON round-trips: rap=exempt ✓, control ✗, pre-fix shape ✗). Next-run check: rap clips show NO `[p-tight]` line. Findings closed: **§7 LLM floor** (16% GPU util — Vulkan split bandwidth-bound, ~48 LLM calls/run, software levers exhausted → model/hardware is the lever) + **§8** single-VOD caveat + final #5/#6 disposition (prod speed = #1+#2 only). Kill clean: 0 procs, VRAM 852 MiB — [[concepts/pipeline-speed-findings-2026-07]] — [[log]]
- [2026-07-09] **BUG 66 first-fix was INCOMPLETE → complete fix (confirm run in flight)**: combined-review run showed `scored_moments` primary_pattern=None 10/10 + a rap clip (T=9832) trimmed AGAIN. Real gap: Stage-4 `hype_moments` entry never emitted primary_pattern (only the trace had it) → Stage 6 preserved None. Fix: emit it in the hype_moments entry (4→6→7). temp-0 `bug66-confirm` run launched to verify (first fix passed synthetic but failed live). Also: card-parallel Stage-4 only ~4% faster (cards cheap; real lever is moment-parallel, held) — [[concepts/bugs-and-fixes#BUG 66]] — [[log]]
- [2026-07-09] **#3 bench INCONCLUSIVE + combined review run launched**: vision-slot bench (conc 1-4) contradicted on sign across runs (noise-dominated at n=4-6) → keep STAGE6/JUDGE workers=2, "raise 2→4" retired as marginal. Combined production run on 2xRaKai launched (bounded): card-parallel(#5 spot-check) + P-TIGHT(re-val Disney/Coke/Wimpy + BUG-66) + judge-timeline(Run 2) + count-shadow. Near-miss candidates surfaced for owner flagging (labels can't be fabricated) — [[concepts/pipeline-speed-findings-2026-07]] — [[log]]
- [2026-07-09] **#5 caveat REFRAMED — concurrency noise < existing sampling noise**: two SERIAL temp-0.3 runs of the same rakai VOD overlap only **5/10** clips (normal run-to-run variance) vs card-parallel's **7/10** at temp 0 → concurrency changes outputs LESS than a plain re-run already does. Enabling `CLIP_PASSB_CARD_WORKERS` = defensible w/ one-batch owner spot-check; A/B-variance (not byte-hash) is the right gate for future moment-parallel — [[concepts/pipeline-speed-findings-2026-07]] — [[log]]
- [2026-07-09] **#6 vectorized scan BUILT + VALIDATED but DEFAULT-OFF (dominated by #2)**: `_scan_vectorized` (`AUDIO_EVENTS_VECTOR`, block-HPSS for music, crowd/rhythmic exact, near-threshold hybrid). 2 real VODs (rakai+Tylil) → **0 fire flips** (band raised 0.05→0.15 after a 0.146 block-HPSS delta on Tylil). Benchmark: ~1.9× over serial — **slower than the shipped threaded default #2 (~3.3×)** → correct+validated but not worth enabling. I6.0–I6.3 cleared — [[concepts/plan-speed56-execution-2026-07]] — [[log]]
- [2026-07-09] **Speed findings consolidated** → [[concepts/pipeline-speed-findings-2026-07]] (measured reference): 21-run timing baseline + `run_metrics.jsonl`; byte-identical wins #1/#2/#7; the LLM-concurrency non-determinism landmine; whisper-already-batched (~200s); dual-GPU one-model-split reality; validation-method learnings — [[log]]
- [2026-07-09] **#5 card-parallel cut-over shipped + validated → KEY NEGATIVE RESULT**: temp-0 hash gate FAILED (cardpar `78594f46…` ≠ baseline `8e031662…`, 23/25 chunks) — chunk 1 (no prior-context) matches, all diffs in summaries; cause = **concurrent temp-0 LLM gen ≠ sequential (LM Studio batching reorders FP) → cards differ even at temp 0**, inherent to any LLM-parallel path. Consequence: different clip set (7/10 shared, 3 swapped). LLM-parallel can't be byte-validated, only owner-quality-judged → **#5 stays default-off; cut-over 2 (moment-parallel) hits same wall, hold**. Byte-identical wins already shipped: #1/#2/#7 — [[concepts/plan-speed56-execution-2026-07]] — [[log]]
- [2026-07-08] **#5 I5.0 shipped + baseline CAPTURED (exit 0)**: prompt-hash instrumentation + `CLIP_PASSB_DETERMINISTIC=1`; golden temp-0 manifest = 25 chunks, fp `8e0316624c64089a`, saved to `learning/passb_baseline/`. Cut-over (I5.2) gated against it; must extract a sequential gate/shape pre-pass first (loop far more stateful than driver model), incremental-with-live-gate. Open: temp-0 reproducibility (first cut-over run = workers=1 must re-match fp) — [[concepts/plan-speed56-execution-2026-07]] — [[log]]
- [2026-07-08] **#5 engine PROVEN (logic) + #6 harness done, #6 deferred**: `passb_driver.py` (serial+two-phase, injected) + `passb_equiv.py` **PASS** — two-phase byte-equivalent to serial (6 sizes×4 workers). Mock proof CAUGHT a real bug pre-live (summary gated on moment-call success → transient failure shortens prior-context) → fixed with exact reconciliation pass. #5 remaining = LIVE stage4 cut-over + live gates (engine done). #6 `vector_equiv.py` harness done (0 flips); **DSP refactor DEFERRED** — #2 threads(default 4)+#1 cache already collapsed scan cost (~3min gain not worth it) — [[concepts/plan-speed56-execution-2026-07]] — [[log]]
- [2026-07-08] **Speed #2 threaded scan NOW DEFAULT-ON** (owner: turn on the safe win): `_resolve_thread_count()` default 0→min(4,cores-2)=4; BLAS-pinned via threadpoolctl (no oversubscription); re-verified byte-identical to serial, 3.3×. Every run now threads the audio scan (no env needed); `--audio-workers 1` obsolete, `AUDIO_EVENTS_THREADS=1` forces serial — [[concepts/plan-pipeline-speed-2026-07]] — [[log]]
- [2026-07-08] **Speed plan implemented — #1/#2/#7 shipped+verified, #3 tool, #4 already-done**: #1 cache audio-events (`stage2.py`, kills ~10-16min rescan/re-run); #2 threaded scan (`AUDIO_EVENTS_THREADS`, in-process/no-spawn — **byte-identical, 3.3×**); #7 `run_metrics.jsonl`+`run_metrics.py` (21 runs backfilled); #3 `bench_vision_slots.py` (ready to run); **#4 parallel renders ALREADY DONE** (`STAGE7_WORKERS`=4 since 2026-06-04 — plan premise wrong). #5 two-phase Pass B STAGED (default-safe, needs live equivalence run); #6 deferred — [[concepts/plan-pipeline-speed-2026-07]] — [[log]]
- [2026-07-08] **Speed plan → detailed 7-item implementation spec** ([[concepts/plan-pipeline-speed-2026-07]]), grounded in the 21-run timing distribution (realtime median 0.262; timing WAS persisted — earlier claim corrected; Pass-B chunks NOT independent — prior-context dependency found, naive threading rejected). #1 cache audio-events / #2 thread scan / #3 slot bench / #4 parallel renders / #5 two-phase Pass B (prompt-hash equivalence proof) / #6 vectorize scan / #7 run_metrics.jsonl+backfill. Projection: re-run ~50→~25 min, fresh ~66→~33 min, all quality-neutral — [[log]]
- [2026-07-08] **Owner review → BUG 66 + fixes + promotions**: 7/10 clips good (Shower Bluff validated); review exposed **BUG 66** (Stage-6 rebuild drops `primary_pattern` → P-TIGHT rap exemption never fired live) — fixed + `head_min_lead` 2→4 + `head_min_sentences=2` + segment-type exemption (synthetic PASS). 9 labels filed+frozen (4 runs/20). Ranking gate still REJECT (confirmatory positives). **Count stays SHADOW** — its one real trim (Disney t=9926) was owner-GOOD; every trimming τ now unsafe per sweep. Title-decoherence warn added (>8s head cut) — [[concepts/bugs-and-fixes#BUG 66]] — [[log]]
- [2026-07-08] **Durable label store — trace pile safe to clean**: labels were pointers into gitignored traces; `label_store.py` freezes each labeled run's full candidate set + labels into committed `learning/frozen_runs/` (features only). `fit_ranker --frozen` trains/gates trace-independently (verified identical verdict). `prune_traces.py` safe-deletes junk traces (124/132 ~28 MB prunable, refuses to orphan a label). Survives disk wipe / fresh checkout — [[concepts/plan-learning-activation-2026-07]] — [[log]]
- [2026-07-03] (older Phase 0-6 build/planning entries — autonomous-loop proof, chat_mine, meme_match, event_timeline — pruned; full record in [[log]])

## Landmines (top gotchas for the next agent)
- **A model loaded with thinking ON wedges Stage 4** (BUG 67) — it burns the 8000-tok budget on `<think>` → empty JSON → 100% failure. gemma-4-26b's thinking is controllable at the TEMPLATE level (fresh load = 0 reasoning, works); the request kwarg only reaches compliant models (qwen). The **fail-fast guard** (`common.preflight_thinking`) now aborts such a run in ~1 s; the **`CLIP_ENABLE_THINKING` dashboard toggle** (default off) drives the request-level lever. Don't toggle thinking ON in LM Studio for the clip model. — [[concepts/bugs-and-fixes#BUG 67]]
- **Double-launch now GUARDED (BUG 67 fixed)** — `is_pipeline_running()` checks the on-disk `pipeline.pid` marker cross-process, so a second/restarted dashboard's play returns 409 and `api_stop` kills via the marker. (Root cause was the per-process in-memory guard missing a run launched by a different/restarted dashboard.) Still prefer one dashboard. — [[concepts/bugs-and-fixes#BUG 67]]
- **`CLIP_USE_DOCKER=1` bypasses the entire Python pipeline** — the dashboard then runs legacy bash `clip-pipeline.sh`, so NONE of Wave 1/2 speed wins (or any run_pipeline.py behavior) apply, and multi-VOD runs become separate per-VOD invocations (C1 can't span them). Bare-metal is the default + the only path that gets the optimizations — [[entities/dashboard]]
- **Speculative decoding is a MEASURED 8× REGRESSION on this dual-GPU rig — do NOT enable it.** With "Use LM Studio Engine protocol" ON the draft flag loads, but decode drops 50→6 tok/s (draft pays the cross-vendor Vulkan coordination tax = the §7 floor). Engine-protocol-no-draft = 51.7 tok/s (neutral). Only revisit on a single card. Draft qwen3.5-2b kept on disk — [[concepts/pipeline-speed-findings-2026-07]] §9
- **LLM-call parallelization is NOT byte-reproducible even at temp 0** — concurrent requests hit LM Studio's batched inference (FP-reduction reorder) → different tokens → different clip set (proven: 7/10 shared). No LLM-parallel speedup can be hash-validated; only owner quality review. Byte-safe speed wins are non-LLM only (threaded DSP scan, caching) — [[concepts/pipeline-speed-findings-2026-07]]
- **phase_runner watchdog false-alarms on full runs** — Stage 5.5 judge runs SILENT ~11 min (batched logging) + `--force` temp-0 runs exceed 60 min. Use `--timeout` ≥5400s, `--stall` ≥900s; the run is usually healthy — check log mtime before assuming a hang — [[concepts/pipeline-speed-findings-2026-07]]
- `panns_inference` **deadlocks (uncatchable stall) on torch≥2.9** in `SoundEventDetection.__init__`, CUDA *and* CPU — it's opt-in (`CLIP_AUDIO_SENSE_PANNS=1`); CLAP is the default audio backend. Also: panns shells out to `wget` (absent on Windows) → pre-place its ckpt+CSV in `~/panns_data/` — [[entities/audio-sense-module]]
- Downloaded TikToks carry a ~3s **outro** (logo + @handle) that clip-forensics mis-logs as real edits — pass `--trim-end 4` / `CLIP_FORENSICS_TRIM_END=4` when batch-analyzing TikTok downloads — [[entities/audio-sense-module]]
- FFmpeg `fade=...:color=white` holds the colour OUTSIDE its ramp window — use transient `drawbox enable='between(t,a,b)'` instead (BUG 64) — [[concepts/transition-animations]]
- Stitch-short needs ≥3 same-category eligibles ≤28s within budget; the invariant is `target+4 ≥ min×cap` (BUG 63) — [[concepts/bugs-and-fixes]]
- `config/originality.json` is untracked dashboard runtime state — edit `DEFAULT_ORIGINALITY` / `config/originality.example.json` for committed defaults — [[entities/dashboard]]
- A single clip can't straddle a chunk seam — `parse_llm_moments` clamps each to its nominal window; A1/M3 emit payoff-centered clips — [[concepts/clip-duration]]
- The Discord **agent** model (`qwen3.5-9b`, in `config/openclaw.json`) is NOT the **pipeline** model (`qwen3.6-35b-a3b`, in `config/models.json`) — two different models, two different files — [[entities/qwen35]]
- NVENC accelerates encode only; per-clip filtering (blur-fill/captions) stays on CPU, so wall-clock gain depends on the filter/encode split — [[concepts/clip-rendering]]
